{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "outputs": [
        "dist/**"
      ],
      "inputs": [
        "src/**/*.{vue, ts, js, css}",
        "!src/__tests__"
      ],
      "persistent": true,
      "dependsOn": [
        "^build"
      ]
    },
    "preview": {
      "cache": false,
      "outputs": [
        "dist/**"
      ],
      "inputs": [
        "src/**/*.{vue, ts, js, css}",
        "!src/__tests__"
      ],
      "dependsOn": [
        "^build"
      ]
    },
    "build": {
      "cache": false,
      "outputs": [
        "dist/**"
      ],
      "inputs": [
        "src/**/*.{vue, ts, js, css}",
        "!src/__tests__"
      ],
      "dependsOn": [
        "^build"
      ]
    },
    "test": {
      "cache": false,
      "outputs": [
        "coverage/**"
      ],
      "inputs": [
        "src/**/*.{vue, ts}",
        "!src/exports",
        "!src/scripts"
      ]
    },
    "lint": {
      "cache": false
    },
    "lint:fix": {
      "cache": false
    },
    "publish": {
      "dependsOn": [
        "build"
      ]
    }
  }
}
